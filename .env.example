NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_SITE_URL=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Comma-separated account emails that may open /admin (all generated models).
# Must be set for the Convex deployment: Dashboard → Settings → Environment Variables,
# or `npx convex env set ADMIN_EMAILS "you@example.com,other@example.com"`.
ADMIN_EMAILS=

# Refinement chat: OpenAI-compatible Chat Completions (`/v1/chat/completions`).
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
# Examples:
# OpenRouter: OPENAI_BASE_URL=https://openrouter.ai/api/v1  (+ their API key header if required)
# Gemini (OpenAI compat): OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

MESHY_API_KEY=
MESHY_BASE_URL=https://api.meshy.ai
MESHY_MODEL=meshy-5
MESHY_USE_MOCK=false
# Preview mesh quality (optional):
# Default: standard mesh, should_remesh off = highest-precision geometry from Meshy (larger GLBs).
# MESHY_PREVIEW_MODEL_TYPE=lowpoly   # stylized low-poly (ignores remesh / polycount)
# Smaller downloads: enable remesh + cap polygons
# MESHY_PREVIEW_SHOULD_REMESH=true
# MESHY_PREVIEW_TARGET_POLYCOUNT=120000
# MESHY_PREVIEW_TOPOLOGY=triangle
# This environment variable is used in Production and Preview environments to deploy your Convex project
# This variable is not needed in development.
# You can create a Production or Preview deploy key at https://dashboard.convex.dev/project/settings#production-deploy-keys
CONVEX_DEPLOY_KEY=
